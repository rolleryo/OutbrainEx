import json
with open(input.json, 'r') as myfile:
    line = myfile.readline()
    data_list = line.split(",")
    for x in data_list 
    if not integer then put surround with ""
    x.replace(' ',':')

    {
    "name": data_list[0],
    "size": data_list[1]
    }
    #,
    # "free": free,
    # "path": path,
    # "log":  log,
    # "port": port,
    # "guid": guid,
    # "clusterUuid": cluster,
    # "disks": disks,
    # "dare": dare
    # }